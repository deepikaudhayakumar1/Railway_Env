[project]
name = "railway-env"
version = "0.1.0"
description = "Railway environment for OpenEnv"
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv>=0.1.13"
]

[project.scripts]
start = "app:app"
